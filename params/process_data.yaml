params:
  features: all # all or list of columns
  train_size: 0.1 # float of share or int of rows
  cols_to_drop: []
  cats: [workclass, education, marital.status, occupation, relationship, race, 
      sex, native.country]
  cats_encoder:
    name: OneHotEncoder # OrdinalEncoder or OneHotEncoder
    params: {handle_unknown: ignore, sparse_output: false}
  num_scalers:
    name: MinMaxScaler # MinMaxScaler or StandardScaler or null
    params: {}
